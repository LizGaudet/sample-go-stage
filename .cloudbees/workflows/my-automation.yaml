apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - test
jobs:
  Checkout:
    steps:
      - uses: cloudbees-io/checkout@v1
        name: checkout
